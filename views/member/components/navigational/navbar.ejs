<nav class="navbar navbar-expand-lg navbar-dark fixed-top custom-navabr">
  <div class="container">

    <!-- Logo -->
    <a class="navbar-brand fw-semibold" href="/">
      Logo
    </a>

    <!-- Toggle -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Menu (TENGAH) -->
    <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
      <ul class="navbar-nav gap-3">
        <li class="nav-item">
          <a class="nav-link active" href="/member/dashboard">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/member/catalog">Catalog</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/circulation">Circulation</a>
        </li>
      </ul>
    </div>

    <!-- RIGHT SIDE (PROFILE DROPDOWN) -->
    <div class="d-flex align-items-center gap-3">

      <div class="dropdown">
        <a
          href="#"
          role="button"
          data-bs-toggle="dropdown"
          aria-expanded="false"
          class="d-inline-flex justify-content-center align-items-center rounded-circle bg-white text-primary overflow-hidden"
          style="width: 40px; height: 40px"
        >
          <% if (currentUser && currentUser.photo) { %>
            <img
              src="<%= currentUser.photo %>"
              alt="Profile Photo"
              style="width: 100%; height: 100%; object-fit: cover;"
            />
          <% } else { %>
            <i class="bi bi-person-fill fs-5"></i>
          <% } %>
        </a>

        <ul class="dropdown-menu dropdown-menu-end shadow p-3" style="min-width: 250px">
          <% if (currentUser) { %>
            <li class="mb-2">
              <strong><%= currentUser.nama %></strong><br />
              <small class="text-muted"><%= currentUser.email %></small>
            </li>
          <% } %>

          <li><hr class="dropdown-divider" /></li>

          <li>
            <a class="dropdown-item" href="/member/profile">
              <i class="bi bi-person me-2"></i>Profile
            </a>
          </li>

          <li>
            <button
              class="dropdown-item"
              data-bs-toggle="modal"
              data-bs-target="#logoutModal"
            >
              <i class="bi bi-box-arrow-right me-2"></i>Logout
            </button>
          </li>
        </ul>
      </div>

    </div>

  </div>
</nav>
<%- include('../modal/modal-logout') %>